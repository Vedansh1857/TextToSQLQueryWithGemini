# TextToSQLQueryWithGemini

### Created the database locally.
### When the user inputs the query, the LLM app converts it into an SQL query.
### Hits the db and fetch the records matching the query output.
